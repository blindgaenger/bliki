<div class="group">
<% @pages.each do |page| %>
  <div class="<%= page.class.to_s.downcase %>">
    <h2 class="title"><%= link_to page.title, page.link %></h2>
    <small>pageed on <%= page.date %>, filed under <%= tag_list page.tags, '', '' %></small>
    <div class="body">
    <%= page.content %>
    </div>
    <%= "<p><a href='#{page.link}#disqus_thread'>View Comments</a></p>" if Sinatra.options.use_comments %>
  </div>
<% end %>
</div>

<% if !@archives.nil? %>
<div class="group">
  <h2>archives...</h2>
  <% @archives.each do |page| %>
  <h3><%= link_to page.title, page.link %> (<%= page.date %>)</h3>
  <% end %>
</div>
<% end %>

<div class="buttons">
  <%= link_to 'New', '/new', :accesskey => 'n' %>
</div>

